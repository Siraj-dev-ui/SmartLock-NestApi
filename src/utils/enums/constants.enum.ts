export enum DoorStatus {
  LOCKED = 'Locked',
  UNLOCKED = 'UnLocked',
  OPEN = 'Open',
  CLOSED = 'Closed',
}

export enum RoomStatus {
  OCCUPIED = 'Occupied',
  VACANT = 'Vacant',
}

export enum RequestStatus {
  APPROVE = 'Approved',
  REJECT = 'Rejected',
  PENDING = 'Pending',
}

export enum Roles {
  ADMIN = 'Admin',
  SUPERVISOR = 'Supervisor',
  USER = 'User',
}

// export enum Actions
//
