export enum DoorStatus {
  LOCKED = 'Locked',
  UNLOCKED = 'UnLocked',
  OPEN = 'Open',
  CLOSED = 'Closed',
}

export enum RoomStatus {
  OCCUPIED = 'Free',
  FREE = 'Free',
}

export enum RequestStatus {
  APPROVE = 'Approved',
  REJECT = 'Rejected',
  PENDING = 'Pending',
}

// export enum Actions
//
